# DOMAIN_NAME and SUBDOMAIN together determine where n8n will be reachable from
# For Tailscale: Use the full machine.tailnet.ts.net format
# The container uses hostname "n8n" to avoid conflicts with the host Mac
DOMAIN_NAME=your-machine.your-tailnet.ts.net

# The subdomain to serve from (empty for Tailscale - no subdomain support)
SUBDOMAIN=

# n8n will be accessible at: https://your-machine.your-tailnet.ts.net

# Optional timezone to set which gets used by Cron and other scheduling nodes
# New York is the default value if not set
GENERIC_TIMEZONE=America/New York

# Tailscale Authentication Key
# Generate at: https://login.tailscale.com/admin/settings/keys
# Use a reusable key with "Ephemeral" disabled for persistent containers
TS_AUTHKEY=tskey-auth-your-key-here

# Note: SSL_EMAIL not needed with Tailscale certificate resolver
# Tailscale automatically manages certificates via Let's Encrypt
